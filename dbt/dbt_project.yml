name: "atmosync"

version: "1.0"

config-version: 2

profile: "atmosync"

model-paths:
  - models

analysis-paths:
  - analyses

test-paths:
  - tests

seed-paths:
  - seeds

macro-paths:
  - macros

snapshot-paths:
  - snapshots

target-path: target

clean-targets:
  - target
  - dbt_packages

models:

  atmosync:

    staging:
      +materialized: view

    intermediate:
      +materialized: table

    marts:
      +materialized: table